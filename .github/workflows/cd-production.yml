name: Deploy — Production

on:
  push:
    branches: [main]

jobs:
  deploy-railway:
    name: Deploy to Railway (production)
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      # ── Option A: Railway ──────────────────────────────────────────────────
      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy to Railway production service
        run: railway up --service ${{ secrets.RAILWAY_SERVICE_ID_PROD }} --detach
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        continue-on-error: ${{ secrets.RAILWAY_TOKEN == '' }}

      # ── Option B: Docker → VPS ─────────────────────────────────────────────
      # Uncomment this block and remove Option A if deploying to a VPS
      #
      # - name: Login to GitHub Container Registry
      #   uses: docker/login-action@v3
      #   with:
      #     registry: ghcr.io
      #     username: ${{ github.actor }}
      #     password: ${{ secrets.GITHUB_TOKEN }}
      #
      # - uses: docker/setup-buildx-action@v3
      #
      # - uses: docker/build-push-action@v5
      #   with:
      #     context: .
      #     target: prod
      #     push: true
      #     tags: |
      #       ghcr.io/${{ github.repository }}:latest
      #       ghcr.io/${{ github.repository }}:${{ github.sha }}
      #     cache-from: type=gha
      #     cache-to: type=gha,mode=max
      #
      # - uses: appleboy/ssh-action@v1
      #   with:
      #     host: ${{ secrets.PROD_HOST }}
      #     username: deploy
      #     key: ${{ secrets.PROD_SSH_KEY }}
      #     script: |
      #       cd /opt/chatroom
      #       echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
      #       docker pull ghcr.io/${{ github.repository }}:latest
      #       docker compose -f docker-compose.prod.yml up -d --no-deps app horizon reverb
      #       docker compose -f docker-compose.prod.yml exec -T app php artisan migrate --force
      #       docker compose -f docker-compose.prod.yml exec -T app php artisan optimize
